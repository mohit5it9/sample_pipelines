resources:
  - name: app_image1_nginx     # resource friendly name
    type: image
    integration: dockerhub1    #friendly name of your integration from step 1         
    pointer:
      sourceName: ankul/nginx    # replace with your image name
    seed:
      versionName: "master.25"   #specify the tag of your image.
  - name: op_cluster1_dcl    # resource friendly name
    type: cluster
    integration: dclKey   # friendly name of the integration you created         
    pointer:
      sourceName: "dcl-cluster" # name of the actual cluster in GKE

