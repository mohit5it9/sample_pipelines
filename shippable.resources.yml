resources:
  - name: box-sn
    type: image
    integration: dockerhub
    pointer:
      sourceName: library/httpd
      isPull: false
    seed:
      versionName: latest
  
  - name: nightly-trigger
    type: time
    seed:
      interval: "0 19 * * *"  # this field accepts cron like syntax
