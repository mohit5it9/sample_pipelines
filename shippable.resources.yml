resources:
  - name: sn-img-1
    type: image
    integration: gcloud
    pointer:
      sourceName: asia.gcr.io/lithe-camp-182705/nginx
      isPull: true
    seed:
      versionName: "latest"
  - name: sn-gke-service-lb                               #required
    type: loadBalancer                          #required
    integration: gcloud  #Required for use in provision jobs
    pointer:
      sourceName: "xyz-scriptnull-local"  # the service name (metadata.name)
      method: ClusterIP # the service type Kubernetes default: ClusterIP
      clusterName: "cluster-1" # The namespace for the service
      region: "asia-central1-a"
    version:
      ports:
        - name: scriptnull-local-port  # Required for multiple ports, must be a DNS_LABEL
          protocol: TCP  # Kubernetes default: TCP
          port: 8001     # Required.
          targetPort: 8002 # A number in 1-65535 or an "IANA_SVC_NAME"  This is the port to access the pod.
         # nodePort: <integer> # Port exposed on the node.  Kubernetes will auto-allocate by default.
      selector:
        new: scriptnull-local # Required. Labels to match the pod

  
   #- name: sn-kube-service-lb                               #required
   #  type: loadBalancer                          #required
   #  integration: scriptnull-kube  #Required for use in provision jobs
   #  pointer:
   #    sourceName: "xyz-scriptnull-local"  # the service name (metadata.name)
   #    method: ClusterIP # the service type Kubernetes default: ClusterIP
   #    clusterName: "small-cluster" # The namespace for the service
   #    region: "europe-west1-b"
   #    namespace: "temp-namespace-f"
   #  version:
   #    ports:
   #      - name: scriptnull-local-port  # Required for multiple ports, must be a DNS_LABEL
   #        protocol: TCP  # Kubernetes default: TCP
   #        port: 8001     # Required.
   #        targetPort: 8002 # A number in 1-65535 or an "IANA_SVC_NAME"  This is the port to access the pod.
           # nodePort: <integer> # Port exposed on the node.  Kubernetes will auto-allocate by default.
   #    selector:
   #      new: scriptnull-local
